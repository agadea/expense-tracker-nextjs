export async function verifyUserAndCreateToken(email: string, password: string) {
  // ...usa db, bcrypt, jose aquí (idéntico a lo que ya tienes en la route)
  // retorna { ok: true, token, user } o { ok: false, error }
  return { ok: false, error: 'Not implemented' };


}